# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv

# Local data
*.jsonl
*.db
*.db-wal
*.db-shm

# Secrets — NOT committed. The provided testnet key is the account OWNER key (withdrawal-capable),
# so it must stay local. (A trade-only agent-wallet key could be committed for zero-setup; this isn't one.)
.env
